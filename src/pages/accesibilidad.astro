---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Breadcrumb from "../components/Breadcrumb.astro";
---

<Layout
  title="Accesibilidad Universal"
  description="La primera implementación en el mundo que integra accesibilidad desde el núcleo del protocolo de pagos"
  lang="es"
>
  <Navbar selectedItem="accesibilidad" />
  <div class="page-wrapper">
    <main class="main-content" id="main-content">
      <div class="container">
        <section class="page-hero">
          <Breadcrumb
            items={[
              { label: "Inicio", href: "/" },
              { label: "Accesibilidad" }
            ]}
          />
          <h1>Accesibilidad Universal Integrada en el Protocolo</h1>
          <p class="lead">
            La primera implementación en el mundo que integra accesibilidad desde el núcleo del protocolo de pagos, no como añadido posterior.
          </p>
        </section>

        <section class="manifesto">
          <div class="manifesto-content">
            <h2>Nuestro Compromiso con la Inclusión</h2>
            <p class="manifesto-text">
              <strong>La accesibilidad no es una característica opcional.</strong> Es un derecho fundamental.
              Por primera vez en la historia de los protocolos de pago blockchain, hemos construido
              accesibilidad universal directamente en el protocolo, haciendo que cada transacción,
              cada respuesta y cada interacción sea comprensible para todas las personas,
              independientemente de sus capacidades.
            </p>
          </div>
        </section>

        <section class="features-grid">
          <h2>Características de Accesibilidad</h2>

          <div class="feature-card">
            <h3><span role="img" aria-label="Open book">📖</span> Lenguaje Simplificado</h3>
            <p>Todos los mensajes se generan en múltiples niveles de complejidad según estándares <span lang="en">CEFR</span> (A1, A2, B1, B2, C1, C2).</p>
          </div>

          <div class="feature-card">
            <h3><span role="img" aria-label="Eye">👁️</span> Compatible con Lectores de Pantalla</h3>
            <p>Todas las respuestas incluyen resúmenes optimizados para lectura por voz.</p>
          </div>

          <div class="feature-card">
            <h3><span role="img" aria-label="Brain">🧠</span> Adaptación Cognitiva</h3>
            <p>Instrucciones paso a paso para personas con diversidad cognitiva.</p>
          </div>

          <div class="feature-card">
            <h3><span role="img" aria-label="Globe">🌍</span> Multilenguaje</h3>
            <p>Soporte para múltiples idiomas con adaptación automática según preferencias del usuario.</p>
          </div>
        </section>

        <section class="cta-section">
          <h2>Descubre Más sobre el Protocolo</h2>
          <p>Conoce cómo implementamos accesibilidad a nivel de protocolo.</p>
          <a href="/arquitectura" class="button button-primary">Ver Arquitectura Técnica</a>
        </section>
      </div>
    </main>
  </div>

  <style>
    .page-wrapper {
      width: 100%;
      min-height: 100vh;
    }

    .main-content {
      width: 100%;
      max-width: 1400px;
      margin: 0 auto;
      padding: 2rem;
    }

    .container {
      width: 100%;
    }

    .page-hero {
      padding: 2rem 0 3rem;
    }

    .page-hero h1 {
      font-size: 2.5rem;
      font-weight: 800;
      margin-bottom: 1rem;
      line-height: 1.3;
      letter-spacing: 0;
    }

    .lead {
      font-size: 1.125rem;
      color: #cbd5e1; /* AAA Compliant */
      line-height: 1.7;
      max-width: 75ch;
    }

    .manifesto {
      padding: 3rem 2rem;
      background: linear-gradient(135deg, #f8fafc 0%, #e0e7ff 100%);
      border-radius: 16px;
      margin: 2rem 0;
    }

    .manifesto-content {
      max-width: 800px;
      margin: 0 auto;
    }

    .manifesto h2 {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: #1e293b;
      letter-spacing: 0;
    }

    .manifesto-text {
      font-size: 1.125rem;
      line-height: 1.8;
      color: #334155;
      max-width: 75ch;
    }

    .features-grid {
      padding: 4rem 0;
    }

    .features-grid h2 {
      font-size: 2rem;
      margin-bottom: 2rem;
      text-align: center;
      letter-spacing: 0;
    }

    .feature-card {
      padding: 2rem;
      background: #1c1c1c;
      border: 2px solid #353535;
      border-radius: 12px;
      margin-bottom: 1.5rem;
      transition: all 0.3s ease;
    }

    .feature-card:hover {
      border-color: #667eea;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .feature-card:focus-within {
      outline: 2px solid #fbbf24;
      outline-offset: 2px;
    }

    .feature-card h3 {
      font-size: 1.25rem;
      margin-bottom: 0.75rem;
      color: #fff;
      letter-spacing: 0;
    }

    .feature-card p {
      color: #cbd5e1; /* AAA Compliant */
      line-height: 1.7;
      max-width: 75ch;
    }

    .cta-section {
      text-align: center;
      padding: 4rem 2rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 16px;
      margin: 2rem 0;
    }

    .cta-section h2 {
      color: white;
      font-size: 2rem;
      margin-bottom: 1rem;
      letter-spacing: 0;
    }

    .cta-section p {
      color: rgba(255, 255, 255, 0.95);
      font-size: 1.125rem;
      margin-bottom: 2rem;
      max-width: 60ch;
      margin-left: auto;
      margin-right: auto;
    }

    .button {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      min-height: 44px; /* AAA Compliant */
      font-size: 1rem;
      font-weight: 600;
      border-radius: 8px;
      transition: all 0.3s ease;
      text-decoration: none;
    }

    .button-primary {
      background: white;
      color: #667eea;
    }

    .button-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(255, 255, 255, 0.3);
    }

    .button-primary:focus,
    .button-primary:focus-visible {
      outline: 2px solid #fbbf24;
      outline-offset: 2px;
      box-shadow: 0 0 0 4px rgba(251, 191, 36, 0.2);
    }

    @media (max-width: 768px) {
      .page-hero h1 {
        font-size: 2rem;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .feature-card:hover,
      .button-primary:hover {
        transform: none;
      }
    }
  </style>
</Layout>
